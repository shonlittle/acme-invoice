# Invoice Processing Pipeline - Dependencies

# PDF text extraction (Slice 7)
PyPDF2==3.0.1

# Web API backend (Above & Beyond: Web UI)
fastapi==0.115.0
uvicorn[standard]==0.30.6
python-multipart==0.0.9
httpx==0.27.0

# Testing (optional - may already be system-installed)
pytest==8.4.1
